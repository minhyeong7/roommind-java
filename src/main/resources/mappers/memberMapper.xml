<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.roomgenius.furniture_recommendation.mapper.MemberMapper">

    <!-- Result Map -->
    <resultMap id="MemberResultMap" type="com.roomgenius.furniture_recommendation.entity.Member">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="createdDate" column="created_date"/>
        <result property="updatedDate" column="updated_date"/>
    </resultMap>

    <!-- 회원가입 -->
    <insert id="insertMember" parameterType="com.roomgenius.furniture_recommendation.entity.Member" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, phone, address, email, password, role)
        VALUES (
        #{username},
        #{phone},
        #{address},
        #{email},
        #{password},
        COALESCE(#{role}, 'user')
        )
    </insert>

    <!-- 이메일 중복 체크 -->
    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 이메일로 회원 조회 -->
    <select id="findByEmail" resultMap="MemberResultMap" parameterType="string">
        SELECT user_id, username, phone, address, email, password, role, created_date, updated_date
        FROM users
        WHERE email = #{email}
    </select>

    <!-- ID로 회원 조회 -->
    <select id="findById" resultMap="MemberResultMap" parameterType="int">
        SELECT user_id, username, phone, address, email, password, role, created_date, updated_date
        FROM users
        WHERE user_id = #{userId}
    </select>

</mapper>
